{
  "name": "opensecdash-website",
  "private": true,
  "type": "module",
  "scripts": {
    "audit:ci": "node scripts/check-npm-audit.mjs",
    "licenses:generate": "node scripts/generate-third-party-notices.mjs",
    "licenses:check": "node scripts/generate-third-party-notices.mjs --check",
    "docs:dev": "vitepress dev --host 127.0.0.1",
    "docs:build": "npm run licenses:check && node scripts/write-indexnow-key.mjs && vitepress build",
    "indexnow:submit": "node scripts/submit-indexnow.mjs",
    "docs:preview": "vitepress preview --host 127.0.0.1"
  },
  "devDependencies": {
    "vitepress": "^1.6.4"
  }
}
